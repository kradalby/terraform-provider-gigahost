---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "gigahost_account_api_key Resource - Gigahost"
subcategory: ""
description: |-
  Creates a personal API key on the Gigahost account. The secret is returned once at creation and stored in state as a sensitive value; it cannot be recovered later. Destroying the resource revokes the key.
---

# gigahost_account_api_key (Resource)

Creates a personal API key on the Gigahost account. The secret is returned once at creation and stored in state as a sensitive value; it cannot be recovered later. Destroying the resource revokes the key.

## Example Usage

```terraform
resource "gigahost_account_api_key" "example" {
  label = "terraform"

  permissions = {
    servers = {
      mode = "rw"
      all  = true
    }
    dns = {
      mode = "r"
      all  = true
    }
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `label` (String) Human-readable label for the key.

### Optional

- `expires_at` (Number) Unix timestamp of key expiry. `0` or omitted means no expiry. Changing this value forces a new key to be created.
- `permissions` (Attributes) Per-category access permissions. Omitting a category grants no access to it. (see [below for nested schema](#nestedatt--permissions))

### Read-Only

- `contact_id` (String) ID of the contact that owns the key.
- `created_at` (Number) Unix timestamp of when the key was created.
- `id` (String) Gigahost's internal key ID.
- `prefix` (String) Non-secret prefix of the key (safe to display).
- `secret` (String, Sensitive) The full API key secret. Shown once at creation; store it immediately.
- `status` (String) Key status: `active` or `revoked`.

<a id="nestedatt--permissions"></a>
### Nested Schema for `permissions`

Optional:

- `account` (Attributes) (see [below for nested schema](#nestedatt--permissions--account))
- `billing` (Attributes) (see [below for nested schema](#nestedatt--permissions--billing))
- `dns` (Attributes) (see [below for nested schema](#nestedatt--permissions--dns))
- `racks` (Attributes) (see [below for nested schema](#nestedatt--permissions--racks))
- `servers` (Attributes) (see [below for nested schema](#nestedatt--permissions--servers))
- `support` (Attributes) (see [below for nested schema](#nestedatt--permissions--support))
- `webhosting` (Attributes) (see [below for nested schema](#nestedatt--permissions--webhosting))

<a id="nestedatt--permissions--account"></a>
### Nested Schema for `permissions.account`

Required:

- `all` (Boolean) When `true`, the permission applies to all resources of this category.
- `mode` (String) `r` for read-only or `rw` for read-write.

Optional:

- `ids` (List of String) Explicit resource IDs the permission applies to when `all` is `false`.


<a id="nestedatt--permissions--billing"></a>
### Nested Schema for `permissions.billing`

Required:

- `all` (Boolean) When `true`, the permission applies to all resources of this category.
- `mode` (String) `r` for read-only or `rw` for read-write.

Optional:

- `ids` (List of String) Explicit resource IDs the permission applies to when `all` is `false`.


<a id="nestedatt--permissions--dns"></a>
### Nested Schema for `permissions.dns`

Required:

- `all` (Boolean) When `true`, the permission applies to all resources of this category.
- `mode` (String) `r` for read-only or `rw` for read-write.

Optional:

- `ids` (List of String) Explicit resource IDs the permission applies to when `all` is `false`.


<a id="nestedatt--permissions--racks"></a>
### Nested Schema for `permissions.racks`

Required:

- `all` (Boolean) When `true`, the permission applies to all resources of this category.
- `mode` (String) `r` for read-only or `rw` for read-write.

Optional:

- `ids` (List of String) Explicit resource IDs the permission applies to when `all` is `false`.


<a id="nestedatt--permissions--servers"></a>
### Nested Schema for `permissions.servers`

Required:

- `all` (Boolean) When `true`, the permission applies to all resources of this category.
- `mode` (String) `r` for read-only or `rw` for read-write.

Optional:

- `ids` (List of String) Explicit resource IDs the permission applies to when `all` is `false`.


<a id="nestedatt--permissions--support"></a>
### Nested Schema for `permissions.support`

Required:

- `all` (Boolean) When `true`, the permission applies to all resources of this category.
- `mode` (String) `r` for read-only or `rw` for read-write.

Optional:

- `ids` (List of String) Explicit resource IDs the permission applies to when `all` is `false`.


<a id="nestedatt--permissions--webhosting"></a>
### Nested Schema for `permissions.webhosting`

Required:

- `all` (Boolean) When `true`, the permission applies to all resources of this category.
- `mode` (String) `r` for read-only or `rw` for read-write.

Optional:

- `ids` (List of String) Explicit resource IDs the permission applies to when `all` is `false`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Find the key ID (the ID column, not the prefix):
#   gigahost account api-keys list
# NOTE: the secret is shown only once at creation and cannot be recovered by
# import. After import the secret attribute will be null in state; manage the
# secret out-of-band and supply it via a variable if needed.
terraform import gigahost_account_api_key.example KEY_ID
```
